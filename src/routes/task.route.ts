/*
import * as express from "express";
import { controller} from "../controllers/task.controller";

const taskRouter = express.Router();

taskRouter.get("/task", controller.getTask);
taskRouter.post("/task", controller.createTask);
taskRouter.put("/task/voteNo", controller.updateNo);
taskRouter.put("/task/voteYes", controller.updateYes);
taskRouter.put("/task/updateTask", controller.updateTask);
taskRouter.put("/task/unvote", controller.updateUnvote);
taskRouter.delete("/task", controller.deleteTask);

export { taskRouter };
*/